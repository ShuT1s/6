#Вводятся К целых чисел. Составьте все возможные списки из этих чисел.
import itertools
c=0
v=0
s=0
l=0
k = input("Введите целые простые числа, разделенные пробелами: ")
while k=='':
    print('Вы ничего не ввели, введите одно или несколько целых простых чисел')
    k = input("Введите целые простые числа, разделенные пробелами: ")
while c==0:
    a = k.split()
    b = len(a)
    for i in a:
        try:
            z=int(i)/int(i)
            if z==1:
                c+=1
        except ValueError:
            a=[]
            print('Вы ввели одно или несколько не целых не простых чисел, или же ввели один или несколько других символов, вводите только целые простые числа')
            k=input('Введите целые числа, разделенные пробелами: ')
            break
    while l!=b:
        a = k.split()
        for i in a:
            for x in range(2, int(i) // 2 + 1):
                if (int(i) % x == 0):
                    v = v + 1
            if v==0:
                l+=1
            else:
                a = []
                print('Вы ввели одно или несколько не простых чисел, вводите простые числа')
                k = input('Введите простые числа, разделенные пробелами: ')
                break
a=k.split()
b=len(a)
n=b
count=1
m=0
while n!=0:
    count=count*n
    n-=1
while count!=m:
    for i in a:
        s+=int(i)
    m+=1
s=s/(count*b)
com_set = itertools.permutations(a)
for i in com_set:
    print(i)
print('Всего комбинаций: '+str(count))
print('Сумма всех чисел из всех комбинаций, поделённая на количество чисел из всех всех комбинаций: '+str(s))
